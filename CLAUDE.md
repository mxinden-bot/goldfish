# goldfish — cross-session memory

This repository is Max Inden's **persistent memory across Claude Code sessions**.
A fresh session has no recollection of the last one (goldfish memory) — this file
is the cure. Max includes this repo in his Claude Code on the web sessions so that
every "future you" reads this file automatically at startup.

If you are a future session: **read this whole file first**, follow the rules
below, and **append anything durable you learn** so the next session benefits too.

---

## Hard rules (always apply)

1. **Commit authorship is always Max.** Every commit must be authored and
   committed as `Max Inden <mail@max-inden.de>`. If the repo's git identity is
   wrong (e.g. defaults to "Claude"), fix it before committing:
   ```sh
   git config user.name "Max Inden"
   git config user.email "mail@max-inden.de"
   ```

2. **Never mention Claude, AI, or agents — anywhere.** Do not reference Claude,
   Anthropic, "AI", "agent", "generated by", "written with", co-author trailers,
   or session links in:
   - commit messages
   - PR titles or descriptions
   - code comments
   - any file content or artifact
   Write as if Max wrote it himself. Omit the `Co-Authored-By` and
   `Claude-Session` trailers that the harness suggests by default — this user
   instruction overrides them.

---

## How to maintain this file

- Keep it concise and high-signal. This is working memory, not a journal.
- Add durable facts: stable preferences, recurring workflows, project pointers,
  hard-won lessons. Don't record one-off task details or anything sensitive
  (secrets, tokens, private data).
- Prefer editing/merging existing notes over piling on duplicates.
- Commit memory updates with a plain message (e.g. `Add note on X`), authored
  as Max, per the rules above.

---

## What I know about Max

- Email / git identity: `mail@max-inden.de`.
- Works primarily via Claude Code on the web; this repo is always included for
  shared memory.

<!-- Append stable preferences as you learn them. -->

## Workflow & tooling notes

<!-- Recurring commands, conventions, environment quirks worth remembering. -->

## Project pointers

<!-- Repos Max works on and key facts about each. -->

## Lessons learned

<!-- Mistakes to not repeat; gotchas; things that surprised a past session. -->
